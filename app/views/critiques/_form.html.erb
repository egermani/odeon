<%= form_for(@critique) do |f| %>
  <% if @critique.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@critique.errors.count, "error") %> prohibited this critique from being saved:</h2>

      <ul>
      <% @critique.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.hidden_field :user_id, :value => session[:user_id]%>
  </div>
  <div class="field">
    <%= f.hidden_field :review_id, :value => @review.id%>
  </div>
  <div class="keypoint-fields">
  <%= f.select(:keypoint_id) do %>
    <%@review.keypoints.each do |keypoint| -%>
      <%=content_tag(:option, keypoint.name, value: keypoint.id)%>
    <%end%>
  <%end%>
  </div>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
