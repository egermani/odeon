<%= form_for(@review) do |f| %>
  <% if @review.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@review.errors.count, "error") %> prohibited this review from being saved:</h2>

      <ul>
      <% @review.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.hidden_field :user_id, :value => session[:user_id] %>
  </div>
    <div class="field">
    <%= f.hidden_field :movie_id, :value => session[:movie_id] %>
  </div>
  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :thesis %><br>
    <%= f.text_field :thesis %>
  </div>
  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body, size: "100x20" %>
  </div>
  <div class="field">
    <%= f.label :rating %><br>
    <%= f.number_field :rating, min: 0, max: 9 %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
